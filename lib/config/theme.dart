import 'package:flutter/material.dart';
// definimos una clase para el tema de la app
class AppTheme{
  // definimos un metodo que retorna un objeto ThemeData
  ThemeData getTheme() => ThemeData(
    brightness: Brightness.dark

  );

}	